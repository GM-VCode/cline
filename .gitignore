# Python
__pycache__/
*.py[cod]
.venv/
# Logs (se rastrean la carpeta logs/ y .gitkeep, se ignoran los .log)
logs/*.log
logs/**/*.log
!logs/.gitkeep
*.log
*.log.*
server_runner*.log

# Modelos GGUF (arquivos grandes - nunca comitar)
*.gguf

# Temporarios/lixo
anotacao.txt
_tmp*
tmp/
# _*  (retirado: coincidía con data/mongodb/__init__.py y lo ignoraba)
#    Usar _*.tmp si se necesita ignorar temporales con guión bajo
*.env
# runtime de validaciones (se regenera com cada validate.py)
.validations.json
# Archivos de banco/runtime do agente (se regeneran) — se ignoran
# por NOMBRE, pero data/json/ y data/ SE RASTREAN (no ignorar carpetas)
data/json/task-state.json
data/json/validations.json
data/json/agent_runs.json
# __pycache__ en toda parte (incl. data/mongodb/)
__pycache__/